name: Merge V2Ray Subscriptions

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Merge Subscriptions
        run: |
          > merged.txt
          > merged_hiddify.txt
          
          URLS=(
            "https://raw.githubusercontent.com/ebrasha/free-v2ray-public-list/refs/heads/main/V2Ray-Config-By-EbraSha.txt"
            "https://raw.githubusercontent.com/hans-thomas/v2ray-subscription/refs/heads/master/servers.txt"
            "https://raw.githubusercontent.com/mahdibland/V2RayAggregator/master/Eternity.txt"
            "https://raw.githubusercontent.com/peasoft/NoMoreWalls/master/list.txt"
            "https://raw.githubusercontent.com/freefq/free/master/v2"
            "https://raw.githubusercontent.com/mfuu/v2ray/master/v2ray"
            "https://raw.githubusercontent.com/youfoundamin/V2rayCollector/main/mixed_iran.txt"
          )
          
          HIDDIFY_URLS=(
            "https://raw.githubusercontent.com/mfuu/v2ray/master/v2ray"
            "https://raw.githubusercontent.com/hans-thomas/v2ray-subscription/refs/heads/master/servers.txt"
            "https://raw.githubusercontent.com/mahdibland/V2RayAggregator/master/Eternity.txt"
            "https://raw.githubusercontent.com/Pawdroid/Free-servers/main/sub"
            "https://raw.githubusercontent.com/youfoundamin/V2rayCollector/main/mixed_iran.txt"
            "https://raw.githubusercontent.com/resasanian/Mirza/main/sub"
          )
          
          for url in "${URLS[@]}"; do
            echo "Fetching: $url"
            content=$(curl -sL --max-time 30 "$url" || echo "")
            if [ -n "$content" ]; then
              decoded=$(echo "$content" | base64 -d 2>/dev/null || echo "$content")
              echo "$decoded" >> merged.txt
              echo "" >> merged.txt
            fi
          done
          
          for url in "${HIDDIFY_URLS[@]}"; do
            echo "Fetching for Hiddify: $url"
            content=$(curl -sL --max-time 30 "$url" || echo "")
            if [ -n "$content" ]; then
              decoded=$(echo "$content" | base64 -d 2>/dev/null || echo "$content")
              echo "$decoded" >> merged_hiddify.txt
              echo "" >> merged_hiddify.txt
            fi
          done
          
          sed -i '/^$/d' merged.txt
          sed -i '/^$/d' merged_hiddify.txt
          iconv -f utf-8 -t utf-8 -c merged_hiddify.txt > merged_hiddify_clean.txt
          mv merged_hiddify_clean.txt merged_hiddify.txt
          echo "Nekobox lines: $(wc -l < merged.txt)"
          echo "Hiddify lines: $(wc -l < merged_hiddify.txt)"

      - name: Commit and Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add merged.txt merged_hiddify.txt
          git diff --staged --quiet || git commit -m "Update merged subscriptions - $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
